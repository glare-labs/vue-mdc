{
  "name": "@glare-labs/ui",
  "version": "0.0.0",
  "private": "true",
  "type": "module",
  "license": "MIT",
  "author": "bre97-web",
  "workspaces": [
    "packages/ui",
    "packages/dev-app"
  ],
  "scripts": {
    "dev": "npm run dev -w dev-app",
    "compile": "npm run generate -w @glare-labs/ui",
    "generate": "npm run generate -w dev-app",
    "lint": "npm run lint -w @glare-labs/ui"
  }
}
