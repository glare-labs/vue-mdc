import { makeComponentTokens } from '../../../utils/tokens'

export const rippleTokens = makeComponentTokens(
    'ripple',
    [
        'hovered-color',
        'hovered-opacity',
        'pressed-color',
        'pressed-opacity',
    ]
)
