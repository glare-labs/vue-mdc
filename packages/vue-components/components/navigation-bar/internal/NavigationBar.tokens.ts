import { makeComponentTokens } from '../../../utils/tokens'

export const navigationBarTokens = makeComponentTokens(
    'navigation-bar',
    [
        'container-color',
        'container-shape',
        'container-height',
        'container-min-width',

    ]
)
